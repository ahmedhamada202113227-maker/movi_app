import 'package:flutter/material.dart';

class SeriesTab extends StatelessWidget {
  const SeriesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Series Content", style: TextStyle(color: Colors.white)),
    );
  }
}
